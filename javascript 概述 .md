#  javaScritp¸概述

## 程序语言

* 基础逻辑部分
    
    * 变量  数据类型   (数据类型)
    * 分支和循环 运算符  (逻辑操作)
    * 函数(对语言的扩展)


```javaacript
var vr=1.2;              //Number
var vr= '11'             //String
var vr=[1,1]             //数组
var vr={a:1,b:2}         //Object
var vr=function(){}      //Function
var vr=true              //Boolean
var vr=undefined
var vr=null

+  -   *   /  %
===  !==  >   <  <=   >=
&&  ||   !

if()
if()else
if()else if()else if()
swich(x){
	case 1:
	break;
	case2 2:
	break;
	default:
	break;
}

for(var i =0 ,i<100;i++){
	
}
while(){
	
}
do{
	
}while()
function xx(){
	
}
var fn=function(){
	
}
fn(a,b)

* 针对特定用途的部分
  *当js来浏览器运行的那一刻
  *浏览器会创建一个window对象
  *window对象中很多属性和方法
  *这些属性和方法不用加window就可以使用

  setInterval

  document.getElementById()
### 选取元素
*  var el=document.getElementById()
*  var el=document.getElementsByClassName()
*  var el=document.getElementsBy
###  筛选元素
*  el.parentNode

###  操作样式 
el.style.color="red";

###  获取位置信息

###  操作属性

###  节点操作



```